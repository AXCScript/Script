local KeysCheckSQL = {
    "1",
    "2",
    "5",
}

local counter = 1
local keyCheck
for i,v in pairs(KeysCheckSQL) do
   if counter == #KeysCheckSQL then
       KeysCheckSQL = ""
       game.Players.LocalPlayer:Kick("please enter your key")
   else
       if v == getgenv().Key then 

return library, library_flags, library.subs
           
       else
           counter = counter +1
       end
   end
end
